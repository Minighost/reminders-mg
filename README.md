A simple reminder app built with Python's Tkinter.

I needed something similar to [SimpleReminder](https://github.com/felixwiemuth/SimpleReminder), but for Desktop.

Probably not the most well written or the prettiest, but it works.

It does *not* persist reminders after closing the window. That'll be future me's problem.
